SHELLCONFIG : config.ini

## other environment variables if you want 
ENV :
    TESTENV : testenv

pureScale configuration :
    NOTE : pureScale configuration 

    Prereq. packages installation : psPrereq.sh
    Db2 pureScale installation on all hosts : db2Install.sh   
    create instance user : crInstUsr.sh   
    configure /var/ct/cfg/netmon.cf : netmon.sh
    setup iscsi target : iscsiTarget.sh
    setup iscsi member clients : iscsiMember.sh
    create a pureScale instance : pSCreateInstance.sh

pureScale operations : 
    NOTE : MISC operation 
    
    Monitor pureScale instance creation : psInstCrtMon.sh
    pureScale stop all nodes for maintenance : psOps.sh stop
    pureScale start all nodes after maintenance : psOps.sh start
    iscsi initiator configuration check : iscsiOps.sh check
    iscsi initiator configuration check : iscsiOps.sh check
    iscsi initiator restart : iscsiOps.sh restart
    iscsi disk initialize (Wipe out enough first blocks. Be careful !!)  : iscsiOps.sh zeroinit
    instance user ksh profile configuration (run only once )  : postOps.sh



